import { InputDecimalDirective } from './input-decimal.directive';

describe('InputDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
